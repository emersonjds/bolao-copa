# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/.idea
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/playwright-report
/test-results
/playwright/.cache
/tests/e2e/.auth

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env / secrets — NUNCA commitar (banco, chaves, tokens)
.env
.env.*
!.env*.example
.dev.vars
.dev.vars.*

# vercel
.vercel

# cloudflare / opennext
.open-next
.wrangler
cloudflare-env.d.ts

# supabase — segredos e artefatos locais
supabase/.env
supabase/.branches
supabase/.temp
**/.env

# typescript
*.tsbuildinfo
next-env.d.ts

# ferramentas de IA / editor
.claude/*
!.claude/agents/
!.claude/agents/**
!.claude/settings.json
.claude/settings.local.json
.mcp.json.local
.serena/
skills-lock.json

# Vídeos de demonstração (gerados localmente, não versionados)
demo/*.webm
test-results/demo-raw/
demo/*.mp4

# backups locais de teste (os reais vivem no repo privado backup-bolao-da-copa)
/backups/
